# Build

To build the project, run `make` in the project root directory.
A binary `tlua` should be created in the root directory.

## Compile Commands

Use `bear` to generate `compile_commands.json`.
```
bear -- make
```

# Testing

Requires the `catch2` package to be installed (on Arch, `pacman -S catch2`).
